allow init efs_file:dir mounton;
allow init mnt_vendor_file:dir mounton;

allow init tmpfs:lnk_file create;

allow init kernel:system module_request;

allow init socket_device:sock_file { create setattr unlink };

allow init sysfs_block_writable:file rw_file_perms;

allow init self:capability sys_module;
allow init { vendor_file system_dlkm_file }:system module_load;

allow init vendor_firmware_file:file mounton;

allow init { boot_block_device cache_block_device }:lnk_file relabelto;
